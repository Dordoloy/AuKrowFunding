{% extends 'base.html.twig' %}

{% block title %}{% trans %}Subscription{% endtrans %}{% endblock %}

{% block body %}
    <h1>{% trans %}Subscription{% endtrans %}</h1>

    <table class="table">
        <tbody>
        <tr>
            <th>Id</th>
            <td>{{ subscription.id }}</td>
        </tr>
        </tbody>
    </table>

    <a href="{{ path('subscription_index') }}">{% trans %}back to list{% endtrans %}</a>

    {{ include('subscription/_delete_form.html.twig') }}
{% endblock %}
