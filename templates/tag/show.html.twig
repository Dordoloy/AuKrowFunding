{% extends 'base.html.twig' %}

{% block title %}{% trans %}Tag{% endtrans %}{% endblock %}

{% block body %}
    <h1>{% trans %}Tag{% endtrans %}</h1>

    <table class="table">
        <tbody>
        <tr>
            <th>Id</th>
            <td>{{ tag.id }}</td>
        </tr>
        <tr>
            <th>{% trans %}Name{% endtrans %}</th>
            <td>{{ tag.Name }}</td>
        </tr>
        </tbody>
    </table>

    <a href="{{ path('tag_index') }}">{% trans %}back to list{% endtrans %}</a>

    <a href="{{ path('tag_edit', {'id': tag.id}) }}">{% trans %}edit{% endtrans %}</a>

    {{ include('tag/_delete_form.html.twig') }}
{% endblock %}
